1. Design an Employee class with instance variables id, name, and salary, and write a
constructor that accepts parameters with these exact same names. Inside the
constructor, use the this keyword to correctly assign the input parameters to the
instance variables to resolve the variable shadowing conflict. Create a method to
display the employee's details.

6. Write a class AreaCalculator that contains overloaded methods named calculateArea.
One method should calculate the area of a square (using one side length), another
method should calculate the area of a rectangle (using length and width), and another
method should calculate the area of a circle (using the radius).

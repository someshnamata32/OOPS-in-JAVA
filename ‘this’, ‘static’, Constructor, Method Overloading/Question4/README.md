4. Develop a Student class that automatically assigns a unique, sequential roll number to
every new student using a combination of static and instance variables. Declare a static
variable nextId initialized to 100 and instance variables for name and rollNumber. In
the constructor, assign the rollNumber using the current value of nextId, then
immediately increment nextId so that the next object created gets a new number.
Verify the logic by creating two students, "Alice" and "Bob", ensuring Alice gets ID 100
and Bob automatically gets ID 101.

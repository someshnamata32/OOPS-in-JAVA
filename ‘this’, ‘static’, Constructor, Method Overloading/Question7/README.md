7. Create a class MaxFinder that contains three overloaded methods named findMax.
Each method should take a different type of input. One method should find the
maximum of two integers. One method should find the maximum of two floats. One
method should find the max element out of an array. Demonstrate the usage of these
overloaded methods in the main method.

3. Create a class representing a student with fields for name, age, and roll number. Create
a non-parameterized constructor to set some default values to the name, age and roll.
Define a parameterized constructor to set these values when a new student object is
created. Use a static variable to keep count of number of student objects are created
and display it using a method. Initialize multiple students to demonstrate the use of
different constructor.
